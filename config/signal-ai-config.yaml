# Hermes Agent config for Signal AI
# Copy to /opt/data/config.yaml or ~/.hermes/config.yaml

model:
  default: "google/gemma-4-31b-it"
  provider: "openrouter"
  base_url: "https://openrouter.ai/api/v1"

platform_toolsets:
  telegram: [hermes-telegram, signal_ai]
